diff --git a/include/zephyr/llext/elf.h b/include/zephyr/llext/elf.h
index b0477416a43..d3f5730bc62 100644
--- a/include/zephyr/llext/elf.h
+++ b/include/zephyr/llext/elf.h
@@ -497,7 +497,14 @@ struct elf64_dyn {
 };
 /** @endcond */
 
-#if defined(CONFIG_64BIT) || defined(__DOXYGEN__)
+/*
+ * CONFIG_LLEXT_ELF_CLASS32 forces the 32-bit ELF layout below even when the
+ * kernel is built 64-bit (CONFIG_64BIT=y).  This supports architectures whose
+ * C ABI uses 64-bit pointers but whose toolchain emits ELFCLASS32 relocatable
+ * objects (e.g. KlaussCPU).  Runtime addresses still use uintptr_t (64-bit);
+ * only the on-disk ELF structures are parsed as 32-bit.
+ */
+#if (defined(CONFIG_64BIT) && !defined(CONFIG_LLEXT_ELF_CLASS32)) || defined(__DOXYGEN__)
 /** Machine sized elf header structure */
 typedef struct elf64_ehdr elf_ehdr_t;
 /** Machine sized section header structure */
diff --git a/include/zephyr/llext/llext.h b/include/zephyr/llext/llext.h
index 19fbe7a0b55..eb452a00dc1 100644
--- a/include/zephyr/llext/llext.h
+++ b/include/zephyr/llext/llext.h
@@ -126,6 +126,13 @@ struct llext_load_param {
 	 * the memory buffer, when calculating relocation targets.
 	 */
 	bool pre_located;
+	/**
+	 * Retain the extension's symbol table (ext->sym_tab) after loading so
+	 * the caller can resolve symbols by name with llext_find_sym().  By
+	 * default the symbol table is freed once loading completes (it is only
+	 * needed during relocation).
+	 */
+	bool keep_symtab;
 };
 
 /** Default initializer for @ref llext_load_param */
diff --git a/subsys/llext/Kconfig b/subsys/llext/Kconfig
index d3e85b6cb9f..e948f41f4fa 100644
--- a/subsys/llext/Kconfig
+++ b/subsys/llext/Kconfig
@@ -41,6 +41,16 @@ config LLEXT_TYPE_ELF_SHAREDLIB
 
 endchoice
 
+config LLEXT_ELF_CLASS32
+	bool "Parse extensions as 32-bit ELF (ELFCLASS32) objects"
+	help
+	  Force the llext loader to interpret extension object files using the
+	  32-bit ELF structure layout (elf32_ehdr/shdr/sym/rela) even when
+	  CONFIG_64BIT is enabled. This is required for architectures whose C
+	  ABI uses 64-bit pointers (so CONFIG_64BIT=y) but whose toolchain emits
+	  ELFCLASS32 relocatable objects, such as KlaussCPU. Without this the
+	  loader would size every ELF structure for ELF64 and reject the file.
+
 config LLEXT_HEAP_SIZE
 	int "llext heap memory size in kilobytes"
 	default 8
diff --git a/subsys/llext/llext_link.c b/subsys/llext/llext_link.c
index dcbd441d0e1..349c7a0a152 100644
--- a/subsys/llext/llext_link.c
+++ b/subsys/llext/llext_link.c
@@ -189,11 +189,13 @@ int llext_link(struct llext_loader *ldr, struct llext *ext, bool do_local)
 			}
 			break;
 		case SHT_RELA:
-			/* FIXME: currently implemented only on the Xtensa code path */
-			if (!IS_ENABLED(CONFIG_XTENSA)) {
-				LOG_ERR("Found unsupported SHT_RELA section %d", i);
-				return -ENOTSUP;
-			}
+			/*
+			 * Xtensa uses a separate GOT/PLT code path below; the
+			 * generic path here also handles RELA: it reads the full
+			 * elf_rela_t (including r_addend) and passes it to
+			 * arch_elf_relocate, which is exactly what RELA targets
+			 * such as KlaussCPU expect.
+			 */
 			if (shdr->sh_entsize != sizeof(elf_rela_t)) {
 				LOG_ERR("Invalid entry size %zd for SHT_RELA section %d",
 					(size_t)shdr->sh_entsize, i);
diff --git a/subsys/llext/llext_load.c b/subsys/llext/llext_load.c
index de3cdd6402b..298c7f566bf 100644
--- a/subsys/llext/llext_load.c
+++ b/subsys/llext/llext_load.c
@@ -184,6 +184,24 @@ static int llext_find_tables(struct llext_loader *ldr)
 		}
 	}
 
+	/*
+	 * LLVM's integrated assembler emits a single string table that serves
+	 * as both the section-header string table (e_shstrndx) and the symbol
+	 * string table (symtab->sh_link), whereas GNU binutils emits two
+	 * separate tables. In the merged case the loop above only assigns
+	 * LLEXT_MEM_SHSTRTAB, leaving LLEXT_MEM_STRTAB unset. Recover it from
+	 * the string table referenced by the symbol table's sh_link.
+	 */
+	if (!ldr->sects[LLEXT_MEM_STRTAB].sh_type &&
+	    ldr->sects[LLEXT_MEM_SYMTAB].sh_type) {
+		unsigned int strtab_idx = ldr->sects[LLEXT_MEM_SYMTAB].sh_link;
+
+		if (strtab_idx < ldr->sect_cnt &&
+		    ldr->sect_hdrs[strtab_idx].sh_type == SHT_STRTAB) {
+			ldr->sects[LLEXT_MEM_STRTAB] = ldr->sect_hdrs[strtab_idx];
+		}
+	}
+
 	if (!ldr->sects[LLEXT_MEM_SHSTRTAB].sh_type ||
 	    !ldr->sects[LLEXT_MEM_STRTAB].sh_type ||
 	    !ldr->sects[LLEXT_MEM_SYMTAB].sh_type) {
@@ -313,6 +331,17 @@ static int llext_map_sections(struct llext_loader *ldr, struct llext *ext)
 				continue;
 			}
 
+			/*
+			 * LLVM's integrated assembler merges .shstrtab and the
+			 * symbol .strtab into one section, so LLEXT_MEM_SHSTRTAB
+			 * and LLEXT_MEM_STRTAB can describe the exact same ELF
+			 * range. An identical alias is not an interleaving of
+			 * distinct sections, so it is not a real overlap.
+			 */
+			if (x->sh_offset == y->sh_offset && x->sh_size == y->sh_size) {
+				continue;
+			}
+
 			if (ldr->hdr.e_type == ET_DYN) {
 				/*
 				 * Test all merged VMA ranges for overlaps
@@ -646,9 +675,13 @@ out:
 
 	/* Until proper inter-llext linking is implemented, the symbol table is
 	 * not useful outside of the loading process; keep it only if debugging
-	 * is enabled and no error is detected.
+	 * is enabled, or the caller asked for it via keep_symtab, and no error
+	 * is detected.
 	 */
-	if (!(IS_ENABLED(CONFIG_LLEXT_LOG_LEVEL_DBG) && ret == 0)) {
+	bool keep_symtab = IS_ENABLED(CONFIG_LLEXT_LOG_LEVEL_DBG) ||
+			   (ldr_parm && ldr_parm->keep_symtab);
+
+	if (!(keep_symtab && ret == 0)) {
 		llext_free(ext->sym_tab.syms);
 		ext->sym_tab.sym_cnt = 0;
 		ext->sym_tab.syms = NULL;
